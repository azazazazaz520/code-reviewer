[P1] ReadFile 没有绑定审查快照，能够读取快照目录外的文件
Reviewer 直接使用注册表中的工具函数；ReadFile 根据模型传入的路径读取文件，没有检查路径是否属于本次快照。使用临时测试文件，已复现通过 snapshot/../outside-fixture.txt 读取目录外内容。
模型如果误用路径，可能读取错误版本的代码；如果被待审查内容中的恶意指令诱导，还可能读取后端进程有权限访问的其他文件。工具结果随后会进入模型请求。本次仅验证了测试文件的越界读取，没有验证真实攻击或读取敏感文件。
**建议：**为每次审查绑定工具根目录，统一解析相对路径，并在解析 ..、符号链接后检查实际路径仍位于快照内。
位置：[工具处理器绑定 (line 74)](/E:/code-reviewer/backend/app/engine/reviewers/base.py:74)、[文件读取 (line 12)](/E:/code-reviewer/backend/app/engine/tools/read_file.py:12)。

[P1] 输入被静默截断，Reflection 又无法有效补足覆盖
这里有两个叠加问题：
- 提示词只包含 Diff 的前 4,000 个字符，每个文件内容再截取前 2,000 个字符，没有标明遗漏范围。
- Reflection 根据已经通过 FindingGate 的结果决定是否补充上下文。LLM 的无行号意见先被过滤；留下的意见一般都有行号；空结果和有行号结果又都会停止循环。
在 25 个源码文件的隔离图执行中，分别让 Reviewer 返回空结果、line=0 意见和有效行号意见，三种情况都只收集了首批 20 个文件，执行一轮便返回 complete。构造的大 Diff 中，后续变更内容也确实未进入初始提示词。
模型可能主动调用工具补读，但当前流程没有确保补读发生，也没有记录实际覆盖范围。
**建议：**按文件或 Diff 区块安排审查批次；由未覆盖的变更决定是否继续执行；预算耗尽时记录未覆盖范围。是否发现问题不宜作为覆盖充分性的判断依据。
位置：[输入截断 (line 87)](/E:/code-reviewer/backend/app/engine/reviewers/base.py:87)、[Reflection 终止条件 (line 25)](/E:/code-reviewer/backend/app/engine/nodes/reflection.py:25)。

[P1] FindingGate 尚未真正验证证据、行号有效性和实际影响
当前主要检查字段存在、文件属于变更列表、部分不确定用语及重复标题。对于变更文件中的任意正行号，通常只是标记为 reviewer_context 后继续放行，没有确认该行是否存在、是否与变更有关，也没有验证证据和影响。
已复现：一条定位到 第 999,999 行、原因仅为个人命名偏好的候选意见，通过了 FindingGate，并使报告风险升为 critical。
此外，static_check 等证据类型可由模型输出直接携带，缺少由实际调用来源确定的可信标记。
**建议：**校验快照中的真实位置，要求候选意见关联可复核的代码或工具证据，并由引擎设置证据来源。允许报告关联上下文问题时，也应检查它与本次变更的因果关系。
位置：[FindingGate 放行逻辑 (line 131)](/E:/code-reviewer/backend/app/engine/finding_gate.py:131)。

[P1] Planning 存在审查空白，却仍输出“完整、低风险”报告
Security Reviewer 主要由少量字符串触发。没有命中这些字符串的配置变更可能既不执行 Reviewer，也不执行确定性校验器。
已确认的例子包括：
- nginx.conf 中的 autoindex on;：审查计划为空。
- 工作流中的 permissions: write-all：审查计划为空。
- TSX 中的 dangerouslySetInnerHTML：Planning 只选择 Style Reviewer；CRG 未额外触发时，没有专门的安全审查。
对第一个案例执行节点链后，结果为 checks=[]、reviewer_outputs={}、review_status=complete、risk_level=low。这些修改是否构成缺陷还需要上下文，但流程已经确定跳过了实质检查。
**建议：**明确每类文件的默认审查责任；对缺少适配器或未覆盖的内容记录检查限制。安全关键词可以补充触发，不能独自承担安全审查的选择职责。
位置：[Reviewer 选择规则 (line 103)](/E:/code-reviewer/backend/app/engine/scope.py:103)、[完成状态推导 (line 33)](/E:/code-reviewer/backend/app/engine/reporting.py:33)。

[P2] 路径规范化会破坏隐藏文件和目录名称
lstrip("./") 会连续删除开头的点和斜杠，因此：
- .github/workflows/ci.yml 变成 github/workflows/ci.yml，后面的工作流专用分类无法命中；
- .env 变成 env，最终分类为 unknown。
这会进一步影响 Reviewer 选择和范围识别。即使调整安全关键词，也无法修复这层路径语义丢失。
**建议：**仅移除明确的 ./ 前缀，保留合法的隐藏文件名称，并为 .env 等无普通扩展名的配置文件增加明确分类。
位置：[路径规范化 (line 57)](/E:/code-reviewer/backend/app/engine/scope.py:57)。

[P2] Commit／PR 快照没有正确处理 Git 转义的中文文件名
_git_changed_files() 使用按行输出的 git diff --name-only，没有使用 -z，也没有解码 Git 的带引号路径。
在启用 Git 默认路径转义的临时仓库中，修改 审查.py 后，得到的文件名包含引号和八进制转义。随后源码分类失败，审查计划为空，按该路径读取文件也失败。
**建议：**与工作区快照已有实现保持一致，使用 --name-only -z 读取真实路径；同步处理 Diff 文件头中的路径转义。
位置：[Commit／PR 变更文件读取 (line 373)](/E:/code-reviewer/backend/app/engine/snapshot.py:373)。

[P2] 混合提交的分类没有落实为文件级 Reviewer 输入
Planning 虽然计算了文件类别，但执行 Reviewer 时仍传入完整的 raw_diff 和 changed_files；上下文选择也主要按顺序和预算裁剪。
已复现：同时修改 update/windows.json 和 src/app.ts 时，Style Reviewer 仍收到发布清单的 Diff、文件名及内容。这样会占用源码审查预算，也保留了生成发布元数据主观意见的入口。
**建议：**让 Review Plan 明确记录各 Reviewer 负责的文件和 Diff 区块，在调用时执行过滤；确有必要的关联文件再作为补充上下文传入。
位置：[Reviewer 上下文构造 (line 60)](/E:/code-reviewer/backend/app/engine/nodes/run_reviews.py:60)。

[P2] 修改模型设置后，后续审查可能继续使用旧客户端
Reviewer 注册表保存的是长期存活的实例，每个实例第一次使用模型时又缓存了 self._llm。设置刷新只清空模块级 _llm，没有清除 Reviewer 持有的引用。
已通过对象级复现确认：调用 reset_llm() 后，已经使用过的 Reviewer 仍返回旧客户端。因此修改模型、服务地址或 API Key 后，新审查可能继续使用旧配置；不同 Reviewer 还可能分别持有不同时间创建的客户端。
**建议：**在任务开始时获取并固定本次使用的模型配置，将客户端注入 Reviewer，避免注册表实例跨任务缓存配置。
位置：[Reviewer 客户端缓存 (line 49)](/E:/code-reviewer/backend/app/engine/reviewers/base.py:49)、[设置刷新 (line 333)](/E:/code-reviewer/backend/app/services/settings_service.py:333)。